set(PROJ_NAME bbos-pico)

set(PROJ_VER "0.1")

##########################################################
# The development platform in use. Must match a directory
# within ./platform. Currently only supports the rp2040,
# but my hope is to include the Arduino platform eventually.
set(PLATFORM rp2040)

##########################################################
# The specific board in use. See the platform documentation
# for a full list.
set(BOARD pico)
